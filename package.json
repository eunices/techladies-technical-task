{
  "name": "todoapp",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^3.6.1",
    "rimraf": "^2.5.4"
  },
  "scripts": {
    "start": "concurrently \"cd client && npm install && npm build\" \"cd server && npm install && npm start\""
  },
  "engines": {
    "node": "9.11.1"
  }
}
